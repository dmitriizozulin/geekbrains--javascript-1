'use strict'

// Tf = (9 / 5) * Tc + 32
const t_celsius = 30;
const t_fahrenheit = (9 / 5) * t_celsius + 32;
alert(t_fahrenheit);

const name = 'Василий';
const admin = name;
alert(admin);

alert(1000 + "108");
